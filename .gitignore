# Cursor, Claude Code, etc. — local IDE metadata (match .cursor and similar .c* dot paths)
.c*

# Cross-tool agent skills dir — regenerated by Codex from .claude/skills; local state, same policy as .c*
.agents/

# Compiled binaries — produced by `make build` and `make build-all`.
# Released builds are published as release artifacts, not via the repo.
bin/

# Common Go scratch files.
*.test
*.out
coverage.txt
